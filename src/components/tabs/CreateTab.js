import React, { useState } from 'react';
import {
  Box,
  Paper,
  Typography,
  TextField,
  Button,
  Stepper,
  Step,
  StepLabel,
} from '@mui/material';

const CreateTab = ({
  account,
  library,
  onDeploy
}) => {
  // ... create token state and functions

  return (
    <Box>
      {/* ... create token interface */}
    </Box>
  );
};

export default CreateTab;
